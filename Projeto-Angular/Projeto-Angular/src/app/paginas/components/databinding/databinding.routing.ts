import { Routes } from "@angular/router";
import { DatabindingComponent } from "./databinding.component";

export const Databinding: Routes = [
    {
        path: 'databinding',
        component: DatabindingComponent
    }
]